# All Source Code and Data

1. The source code for the fastText, BERT, and LSTM models is in `nlp/nlp.ipynb`.
2. Two datasets: the Large Japanese Word dataset is `nlp/data/large_japanese_words.txt`, and
the onomatopoeia dataset is `nlp/data/onomatopoeia.csv`.
